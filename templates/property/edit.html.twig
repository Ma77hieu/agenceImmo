{% extends 'base.html.twig' %}
{% block title %} Editer {{property.title}} {% endblock %}
{% block body %}
<div class="container col-md-8">
    <h1> Edition </h1>
    {{ form_start(form)}}
        {{ form_widget(form) }}
        <button type="button" name="edition" id="editproperty" class="btn btn-primary btn-lg btn-block">Editer
        </button>
    {{ form_end(form)}}
    </div>

{% endblock %}